{:adoption/version 3 :consumer/id :kotoba-lang
 :security/git-sha "f409dbade106df076cefe640781b12db558f4135"
 :required-control-namespaces [kotoba.security.abac kotoba.security.approval kotoba.security.capability kotoba.security.crypto-policy kotoba.security.hardware kotoba.security.qualification kotoba.security.resilience kotoba.security.transport]
 :security-sensitive-entrypoints
 {kotoba.lang.release-admission [kotoba.security.abac kotoba.security.approval kotoba.security.capability kotoba.security.crypto-policy kotoba.security.hardware kotoba.security.qualification kotoba.security.resilience kotoba.security.transport]}
 ;; adoption v3 (security#61) requires every def/defn whose name matches
 ;; encrypt|decrypt|authorize|authenticate|credential|private-key|secret|
 ;; revoke|rotate-key|restore-backup|verify-signature|sign-artifact to be
 ;; declared here and bound to a control its own namespace requires. Measured
 ;; against this repo's src: zero matches, so the inventory is empty rather
 ;; than omitted -- the checker distinguishes "declared and empty" from
 ;; "missing", and an empty map is the honest statement.
 :sensitive-operations {}
 :exceptions []}
