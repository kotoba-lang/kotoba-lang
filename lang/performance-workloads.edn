{:kotoba.lang.performance-workloads/version 1
 :profile 4
 :purpose "Representation changes require workload evidence; these cases guard semantic scale and provide repeatable size/runtime inputs."
 :cases
 [{:id :pair-chain-twelve-items
   :entry "conformance/performance/collections_12.kotoba"
   :expect 103
   :measures #{:compile-byte-count :wasm-byte-count :execution-result}
   :observed {:date "2026-07-18"
              :primary-wasm-byte-count 152480
              :primary-function-count 12
              :repeated-compile-bytes-identical true
              :note "Compile latency is environment-dependent and intentionally not frozen as a conformance value."}
   :decision-use "Proves primary collection semantics no longer truncate at eight."}
  {:id :multi-source-map-twelve-items
   :entry "conformance/performance/multi_map_12.kotoba"
   :expect 12
   :measures #{:compile-byte-count :wasm-byte-count :execution-result}
   :status :pending-primary-compact-helper-state
   :decision-use "Release-visible residual gap; do not justify a trie ABI from this case alone."}]
 :abi-decision
 {:current :persistent-pair-chain
  :retain-while "bounded workloads fit release budgets and linear lookup is not dominant"
  :consider-tagged-trie-when
  {:minimum-evidence [:representative-real-programs :allocation-profile
                      :lookup-latency-profile :artifact-size-delta]
   :required-improvement "material end-to-end improvement, not a microbenchmark-only win"}}}
