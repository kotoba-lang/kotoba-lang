{:kotoba.lang.qualification.q6/version 1
 :historical-regressions
 [{:id :reader-eval-rce
   :source "#=(java.lang.System/exit 0) (defn main [] 0)"
   :expect :reject}
  {:id :runtime-require-escape
   :source "(defn main [] (require 'clojure.java.shell))"
   :expect :reject}
  {:id :host-constructor-escape
   :source "(defn main [] (new java.io.File \"/tmp/escape\"))"
   :expect :reject}
  {:id :ambient-mutation
   :source "(defn main [] (let [x (atom 0)] (swap! x inc)))"
   :expect :reject}
  {:id :hidden-exception-effect
   :source "(defn main [] (try 1 (catch Exception e 0)))"
   :expect :reject}]
 :almost-valid
 [{:id :unknown-call :source "(defn main [] (safe-looking-call 1))"}
  {:id :too-deep-reader
   :source-prefix "(defn main [] "
   :repeat "(if 1 "
   :repeat-count 513
   :source-suffix "0 0)"}
  {:id :trailing-top-level-effect
   :source "(defn main [] 0) (println \"ambient\")"}]}
