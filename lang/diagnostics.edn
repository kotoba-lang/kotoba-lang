{:kotoba.diagnostics/version 1
 :coverage :bounded-contract-surface
 :stability
 {:codes :stable-within-language-profile
  :messages :informational
  :unknown-code :preserve-and-report}
 :diagnostics
 [{:code :command/unknown
   :phase :cli
   :summary "The requested command is not in the public CLI contract."
   :action "Use a command generated from lang/cli.edn."
   :source "src/kotoba/cli.cljk"}
  {:code :contract/invalid
   :phase :cli
   :summary "The CLI contract failed structural validation."
   :action "Inspect the structured :errors collection; do not dispatch the command."
   :source "src/kotoba/cli.cljk"}
  {:code :version/unsupported
   :phase :compatibility
   :summary "The requested language or package contract version is unknown."
   :action "Select a version listed under :supported in lang/version-policy.edn."
   :source "src/kotoba/lang/version_policy.cljk"}
  {:code :version/removed
   :phase :compatibility
   :summary "The requested contract version has been removed."
   :action "Migrate to the active version before compiling or running."
   :source "src/kotoba/lang/version_policy.cljk"}
  {:code :version/deprecation-expired
   :phase :compatibility
   :summary "The compatibility window for a deprecated version has expired."
   :action "Apply the migration named by the version policy."
   :source "src/kotoba/lang/version_policy.cljk"}
  {:code :release/invalid-semver
   :phase :release
   :summary "A release identifier is not strict SemVer."
   :action "Use MAJOR.MINOR.PATCH with an optional valid pre-release or build suffix."
   :source "src/kotoba/lang/version_policy.cljk"}
  {:code :docs/no-release-bound-profile
   :phase :documentation
   :summary "No published implementation evidence binds the active language profile."
   :action "Keep the public default blocked until a signed release envelope binds the implementation and profile."
   :source "lang/docs-release.edn"}
  {:code :docs/link-missing
   :phase :documentation
   :summary "A checked document points to a missing local target."
   :action "Restore the target or update the authority map and regenerate the reference."
   :source "scripts/check-docs.cljk"}
  {:code :docs/profile-version-drift
   :phase :documentation
   :summary "Grammar, surface, and elaboration authorities disagree on the language profile."
   :action "Reconcile the authorities before publishing documentation."
   :source "scripts/check-docs.cljk"}
  {:code :docs/generated-drift
   :phase :documentation
   :summary "A committed generated reference does not match its machine authority."
   :action "Run nbb scripts/generate-docs-reference.cljk and commit the result."
   :source "scripts/check-docs.cljk"}
  {:code :docs/validation-result-invalid
   :phase :documentation
   :summary "A user-validation observation is incomplete or overclaims an external result."
   :action "Record participant class, task, outcome, evidence, and observed time."
   :source "scripts/check-docs.cljk"}]
 :non-goals
 #{:exhaustive-compiler-errors :message-string-stability :host-specific-errors}}
