;; CI5 — a core-module receipt is not a component receipt; this binder binds
;; component imports only.
{:receipt {:format :kotoba.wit-package/v1 :target :wasm32-wasi-kotoba-v1 :imports [:component/http]}
 :handler-imports [:component/http]}
