;; CI5 — a handler for an import the component never declared is authority the
;; WIT world does not describe. Refused at binding time.
{:receipt {:format :kotoba.wit-package/v1 :target :wasm-component-kotoba-v1 :imports [:component/http]}
 :handler-imports [:component/http :component/database]}
