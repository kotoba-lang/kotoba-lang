;; CI5 — a receipt from a lowering that does not declare which guard its
;; imports require. Bound on trust, this is where a weaker guard slips in
;; without contradicting anything, so it is refused.
{:receipt {:format :kotoba.wit-package/v1
           :target :wasm-component-kotoba-v1
           :imports [:component/http]}
 :handler-imports [:component/http]}
