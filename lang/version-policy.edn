{:kotoba.lang.version-policy/version 1
 :release/current "0.5.0"
 :release/semver-required true
 ;; T10.1: every release binds the active language profile (surface-status profile-version).
 ;;
 ;; DECLARED DRIFT (2026-08-10): lang/surface-status.edn now says
 ;; profile-version 6, and this number — which the comment above says IS that
 ;; number — still says 5, with 6 absent from :supported below. So a signed
 ;; release tag currently binds a profile the language surface has moved past.
 ;;
 ;; It is left alone on purpose. `kotoba.lang.grammar-authority` now enforces
 ;; agreement for the identity axis (elaboration-pipeline's :contract-versions
 ;; against surface-status), because a stale number there silently mis-seals a
 ;; definition CID. This number is different: promoting it declares profile 6
 ;; RELEASED and adds it to :supported, which is a statement about what the
 ;; project ships, not a copy that drifted. That is an owner decision.
 :release/language-profile 5
 :release/package-contract 1
 :supported
 {:language-profile
  {3 {:status :deprecated
      :deprecated-on "2026-07-23"
      :removal-not-before "2027-01-19"}
   ;; Profile 4 treated booleans as i64 arithmetic values: `(+ (zero? x)
   ;; (pos? x))` was admitted. Profile 5 types comparisons and predicates as
   ;; `:bool`, which is both the Clojure reading and a type error for that sum.
   ;; Migration is mechanical: `(if p 1 0)` where a number was wanted.
   4 {:status :deprecated
      :deprecated-on "2026-07-30"
      :removal-not-before "2027-01-26"
      :breaking-change :bool-typed-comparisons
      :migration "wrap a predicate in (if p 1 0) where an i64 was wanted"}
   5 {:status :active}}
  :package-contract
  {1 {:status :active}}}
 :deprecation
 {:minimum-days 180
  :removal-requires-major true
  :notice-required true}
 :release-tags
 {:prefix "v"
  :algorithm :ed25519
  :external-trust-store-required true
  ;; T10.1: language-profile is part of the signed tag content binding.
  :binds #{:version :commit :tree :source-root :issued-at-ms :language-profile}}
 :compatibility-report
 {:format-version 1
  :unknown-version :deny
  :expired-deprecation :deny
  :stable-order true}
 ;; T10.3: surface-status diffs go in release notes (process, not automation).
 :release-notes
 {:require-surface-status-diff? true
  :surface-status-path "lang/surface-status.edn"
  :surface-matrix-path "docs/lang/surface-matrix.md"}}
