{:kotoba.lang.w6.inventory/version 1
 :generated-at "2026-07-28"
 :updated-at "2026-07-28-connect-cloud-provision"
 :status :accepted-first-slice
 :classification-scheme
 [:portable-pure
  :portable-effectful
  :host-mechanism
  :operational-command
  :blocked-by-language
  :blocked-by-provider]
 :notes
 "W6 first inventory after design-system Delivery-6 (5/5 document cutover) and
  W5 host-kit deepen through ADR 0142. Classifications are repo-level defaults;
  path-level refinements follow per-slice migration."

 :cohorts
 [{:id :design-system
   :title "Design system document cutover"
   :status :complete
   :dependency-order ["css" "html" "shitsuke" "liquid-glass-ui" "kotoba-ui"]
   :repositories
   [{:repository "orgs/kotoba-lang/css"
     :default-class :portable-pure
     :status :document-layer-migrated
     :kotoba-paths ["kotoba/css_core.kotoba" "kotoba/css_document.kotoba"]
     :host-remainder ["src/css/core.cljc"]
     :notes "Logical style :document → CSS stream; form-A oracle remains."}
    {:repository "orgs/kotoba-lang/html"
     :default-class :portable-pure
     :status :document-layer-migrated
     :kotoba-paths ["kotoba/html_core.kotoba" "kotoba/html_document.kotoba"]
     :host-remainder ["src/html/core.cljc"]
     :notes "Logical UI :document → HTML stream; pretty-print stays host."}
    {:repository "orgs/kotoba-lang/shitsuke"
     :default-class :portable-pure
     :status :document-layer-migrated
     :kotoba-paths ["kotoba/tokens_document.kotoba" "kotoba/hig_document.kotoba"]
     :host-remainder ["src/**/*.cljc"]
     :notes "Token groups → CSS custom-property stream."}
    {:repository "orgs/kotoba-lang/liquid-glass-ui"
     :default-class :portable-pure
     :status :document-layer-migrated
     :kotoba-paths ["kotoba/tokens_document.kotoba"]
     :host-remainder ["src/**/*.cljc"]
     :notes "Material tokens → --liquid-glass-* stream."}
    {:repository "orgs/kotoba-lang/kotoba-ui"
     :default-class :portable-pure
     :status :document-layer-migrated
     :kotoba-paths ["kotoba/theme_document.kotoba" "kotoba/theme_core.kotoba"]
     :host-remainder ["src/**/*.cljc"]
     :notes "Theme cascade layer; full theme-css join remains .cljc."}]}

  {:id :language-platform
   :title "Language platform (host authority)"
   :status :not-a-product-file-port
   :repositories
   [{:repository "orgs/kotoba-lang/compiler"
     :default-class :host-mechanism
     :status :w5-deepen-landed
     :portable-subsurfaces ["examples/*.kotoba" "test fixtures"]
     :notes "Frontend/HIR/KIR/runtime authority. Guest product examples under examples/."}
    {:repository "orgs/kotoba-lang/kotoba-kir"
     :default-class :host-mechanism
     :status :active
     :notes "Value/runtime IR; linear resource table ADR 0133."}
    {:repository "orgs/kotoba-lang/kotoba-component"
     :default-class :host-mechanism
     :status :active
     :notes "Component packaging + Wasmtime multi-step evidence ADR 0130–0136."}
    {:repository "orgs/kotoba-lang/abi"
     :default-class :host-mechanism
     :status :active
     :notes "Capability wire IDs and typed operation catalog."}
    {:repository "orgs/kotoba-lang/kotoba-wasm"
     :default-class :host-mechanism
     :status :active
     :notes "Wasm tooling adapter for component packaging."}
    {:repository "orgs/kotoba-lang/provider"
     :default-class :host-mechanism
     :status :w5-kits-landed
     :path-classes
     {"src/provider/object.cljc" :host-mechanism
      "src/provider/http.cljc" :host-mechanism
      "src/provider/*_transport.cljc" :host-mechanism
      "src/provider/conformance.cljc" :portable-effectful}
     :notes "Qualified kits for clock/log/http/storage/llm/object/ui/ingress. Not guest product source."}]}

  {:id :next-product-verticals
   :title "Next product migration cohort (post W5 kits)"
   :status :inventory-only
   :repositories
   [{:repository "orgs/kotoba-lang/murakumo"
     :default-class :blocked-by-provider
     :status :path-inventory-landed
     :path-inventory "lang/w6-murakumo-path-inventory.edn"
     :blocked-on ["state kit production cutover"
                  "llm production transport"
                  "governor/checkpoint product path"]
     :path-hints
     ["src/murakumo/infer/plan.cljc"
      "src/murakumo/task/plan.cljc"
      "src/murakumo/kekkai/gate.cljc"
      "src/murakumo/dash/state.cljc"
      "src/murakumo/token.cljc"]
     :notes "Path-level inventory v1; high-priority pure-planner oracles complete: kekkai#37, infer-plan#38, dash-state#39, task-plan#40, token#41, infer-engine#42. SSH/nbb shells remain host."}
    {:repository "orgs/kotoba-lang/murakumo-studio"
     :default-class :blocked-by-provider
     :status :candidate
     :notes "Studio UX over murakumo; follows murakumo vertical."}
    {:repository "orgs/kotoba-lang/com-cloudflare"
     :default-class :blocked-by-provider
     :status :path-inventory-landed
     :path-inventory "lang/w6-cloudflare-path-inventory.edn"
     :blocked-on ["provider HTTP + secret-custody for API token"
                  "workerd app ingress remains provider ADR 0103 (separate)"]
     :path-hints
     ["src/cloudflare/stream.cljc"
      "src/cloudflare/analytics.cljc"
      "src/cloudflare/workers.cljc"
      "src/cloudflare/zones.cljc"]
     :notes "API v4 client path inventory; pure-request string oracles complete (#1 stream, #2 analytics+paths). Optional parse tallies remain. routes = Workers/DNS/Pages product discovery, not workerd app router."}
    {:repository "orgs/kotoba-lang/com-cloudflare-compat"
     :default-class :host-mechanism
     :status :adapter
     :notes "Compat shim; keep as host-mechanism."}
    {:repository "orgs/kotoba-lang/kotoba-script"
     :default-class :blocked-by-language
     :status :candidate
     :blocked-on ["kbb scoped filesystem"
                  "process"
                  "Git"
                  "secret-custody"
                  "cloud deployment abilities"]
     :notes "Checked KIR→mjs backend today. Ops nbb shells stay until kbb ability gaps close (lang/w6-kbb-ability-gap.edn)."}
    {:repository "orgs/kotoba-lang/kami-engine-script-runtime"
     :default-class :host-mechanism
     :status :adapter
     :notes "Script runtime adapter; not first cutover target."}]}

  {:id :compiler-guest-product-evidence
   :title "Guest product examples already on .kotoba (evidence, not repo ports)"
   :status :landed-with-w5
   :paths
   [{:path "orgs/kotoba-lang/compiler/examples/w5-linear-let-move.kotoba"
     :class :portable-effectful
     :adr "0137"}
    {:path "orgs/kotoba-lang/compiler/examples/w5-object-put-get-product.kotoba"
     :class :portable-effectful
     :adr "0140-0142"
     :notes "put/get/CAS product verticals + conditional get."}]}]

 :next-actions
 [{:id :w6-murakumo-path-inventory
   :description "Path-level classify murakumo governor/checkpoint/state/llm entrypoints."
   :status :done
   :artifact "lang/w6-murakumo-path-inventory.edn"}
  {:id :w6-cloudflare-route-inventory
   :description "Inventory route modules vs mechanism adapters after ingress soak."
   :status :done
   :artifact "lang/w6-cloudflare-path-inventory.edn"}
  {:id :w6-kbb-ability-gap
   :description "Track kbb ability gaps blocking kotoba-script cutover from nbb."
   :status :done
   :artifact "lang/w6-kbb-ability-gap.edn"}
  {:id :w6-cloudflare-pure-request-oracle
   :description "Oracle parity for stream validate/redact + analytics query + path builders."
   :status :high-priority-string-cores-complete
   :landed ["com-cloudflare#1 stream_core" "com-cloudflare#2 analytics_core + workers_path_core"]
   :remaining ["optional map-reduce parse tallies"]}
  {:id :w6-qualify-process-scoped-fs
   :description "Qualify process + scoped-fs kits for kbb ops host (high-priority gaps)."
   :status :os-transports-landed
   :evidence "provider#24 ADR 0143 + provider#25 ADR 0144"}
  {:id :w6-process-os-spawn-transport
   :description "Production OS spawn transport under command allowlist."
   :status :done
   :evidence "provider#25"}
  {:id :w6-scoped-fs-os-mount-transport
   :description "Production scoped root mounts without ambient FS."
   :status :done
   :evidence "provider#25"}
  {:id :w6-secret-custody-ops
   :description "Wire secret-custody into ops CLIs (no ambient env dump)."
   :status :host-transports-landed
   :evidence "provider#26+#27 ADR 0145/0146"}
  {:id :w6-ops-cli-secret-cutover
   :description "Murakumo/cloudflare CLIs consume named secret fetch instead of ambient getenv."
   :status :first-cutover
   :evidence "murakumo#48 + com-cloudflare#3"}
  {:id :w6-cljs-os-transports
   :description "cljs/nbb OS spawn + scoped-fs root mounts under sync contract."
   :status :done
   :evidence "provider#28 ADR 0147"}
  {:id :w6-ssh-host-forever
   :description "Document murakumo fleet SSH as permanent host-mechanism."
   :status :done
   :evidence "docs/w6-ssh-host-forever.md"}
  {:id :w6-secret-getenv-audit
   :description "Classify remaining murakumo getenv call-sites secret vs config."
   :status :first-pass
   :evidence "docs/w6-secret-getenv-audit.md"}
  {:id :w6-ops-cli-secret-remaining
   :description "Cut over SERVICE_TOKEN / METRICS_TOKEN / overlay auth-key to named fetch."
   :status :done
   :evidence "murakumo#50"}
  {:id :w6-ops-cli-quic-secret-pathref
   :description "QUIC cert/key path refs under scoped-fs (prefer over PEM-in-env)."
   :status :done
   :evidence "murakumo#52+#53"}
  {:id :w6-git-kit-contract
   :description "Read-only git kit under subcommand allowlist."
   :status :done
   :evidence "provider#29 ADR 0148"}
  {:id :w6-git-os-run
   :description "JVM git-transport/os-run absolute bin+worktree."
   :status :done
   :evidence "provider#31 ADR 0149"}
  {:id :w6-git-cljs-os-run
   :description "cljs/nbb git os-run via spawnSync."
   :status :done
   :evidence "provider#32 ADR 0150"}
  {:id :w6-git-tooling-cutover
   :description "murakumo deploy pin uses absolute git (no PATH)."
   :status :first-cutover
   :evidence "murakumo#55"}
  {:id :w6-entropy-kit
   :description "CSPRNG entropy kit for ids (closes clock-and-random)."
   :status :done
   :evidence "provider#33 ADR 0151"}
  {:id :w6-cloud-deploy-first-slice
   :description "Pure Workers/Pages deploy request builders under grant."
   :status :done
   :evidence "com-cloudflare#5 ADR 0003"}
  {:id :w6-cloud-deploy-kotoba-oracle
   :description "deploy_core.kotoba validators/paths/wrangler argv oracle parity."
   :status :done
   :evidence "com-cloudflare#6 ADR 0004"}
  {:id :w6-cloud-deploy-module-multipart
   :description "Workers ES-module multipart put plan + live upload."
   :status :done
   :evidence "com-cloudflare#7 ADR 0005"}
  {:id :w6-cloud-deploy-module-kotoba-oracle
   :description "deploy_core module validate + multipart encode oracle."
   :status :done
   :evidence "com-cloudflare#8 ADR 0006"}
  {:id :w6-bare-git-audit
   :description "No bare git on PATH in murakumo ops."
   :status :done
   :evidence "murakumo#58"}]}
