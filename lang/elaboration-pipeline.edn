{:kotoba.lang.elaboration-pipeline/version 1
 :kotoba.lang.elaboration-pipeline/adr
 "adr-2607279200-kotoba-clojure-shaped-safety-elaboration-migration"
 :authority "kotoba-lang/kotoba-lang"
 :source-authority "lang/guest-grammar.edn"
 :identity-authority "lang/code-identity.edn"
 :stages
 [{:id :closed-reader
   :owner :compiler
   :required true}
  {:id :bounded-pure-desugar
   :owner :compiler
   :required true}
  {:id :name-and-module-resolution
   :owner :compiler
   :required true}
  {:id :type-and-schema-inference
   :owner :compiler
   :required true}
  {:id :interprocedural-effect-inference
   :owner :compiler
   :required true}
  {:id :implicit-ability-parameter-elaboration
   :owner :compiler
   :required true}
  {:id :typed-hir-kir
   :owner :compiler
   :required true}
  {:id :definition-cid
   :owner :kotoba-lang
   :required true}
  {:id :restricted-target
   :owner :compiler
   :required true
   :targets #{:restricted-esm :wasm-component}}
  {:id :host-binding
   :owner :host
   :required true
   :requires #{:package-identity :definition-identity :component-identity
               :wit-world :grant :local-policy :quota :deadline :revocation
               :provider-revalidation :receipt}}]
 :source-forbidden
 #{:numeric-capability-id :wit-import :portable-effect-envelope
   :provider-callback :ambient-host-binding}
 :definition-identity-inputs
 #{:profile-version :desugar-contract :type-contract :effect-contract
   :interface :typed-kir :direct-definition-cids}
 :release-gates
 [:source-fidelity :semantic-parity :safety :host-parity :performance
  :release :rollout]}
