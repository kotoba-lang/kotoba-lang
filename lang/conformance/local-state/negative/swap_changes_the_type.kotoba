(ns kotoba.lang.conformance.local-state-negative (:export [main]))
;; refused: atom `a` is i64; this rebinding is string (atom slice 1 requires one type per cell)
(defn main [] :i64
  (let [a (atom 0)]
    (swap! a string-from-i64)
    1))
