(ns kotoba.lang.conformance.local-state-negative (:export [main]))
;; refused: atom `a` escapes its let scope (atom slice 1 admits swap!/reset!/deref in straight-line code of the binding function only)
(defn- takes [x :i64] :i64 x)
(defn main [] :i64 (let [a (atom 0)] (takes a)))
