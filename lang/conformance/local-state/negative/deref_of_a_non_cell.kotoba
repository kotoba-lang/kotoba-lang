(ns kotoba.lang.conformance.local-state-negative (:export [main]))
;; refused: deref expects a let-bound atom cell as its first argument; got `x` (atom slice 1)
(defn- read-it [x :i64] :i64 (deref x))
(defn main [] :i64 (read-it 5))
