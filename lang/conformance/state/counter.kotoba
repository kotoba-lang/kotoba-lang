(defhandler counter [:state :i64]
  (get [s] (resume s s))
  (put [s v] (resume v v)))

(defn- tick [] (perform :state/put (+ 1 (perform :state/get))))

(defn main [] :i64
  (handle (do (tick) (tick) (perform :state/get))
          (with counter 40)))
