(ns ceiling {:kotoba/export [main]})

(defhandler cell [:state :i64]
  (get [s] (resume s s))
  (put [s v] (resume v v)))

(defn- tick [] {:effects #{:state}} (perform :state/put (+ 1 (perform :state/get))))

(defn main [] :i64 (handle (tick) (with cell 41)))
