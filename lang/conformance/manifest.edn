;; GENERATED-ish by T1.2 transform; hand-editable.
;; Pure-product required backends: #{:kir :wasm32-kotoba-v1}
{:kotoba.lang.conformance/version 2,
 :kotoba.lang.conformance/wbs "T1.2",
 :kotoba.lang.conformance/note
 "Required-backends matrix (reliability parity T1.2). Case classes declare which execution/admit backends must pass. Pure-product run cases require KIR + wasm32-kotoba-v1 (T1.3 implements dual-backend runner). Host-compat cases are not a pure-product silent-pass loophole — they are separate classes.",
 :backends
 {:kir
  {:id :kir,
   :role "KIR execute (product pure oracle / kotoba.kir)",
   :status :required-for-pure-product},
  :wasm32-kotoba-v1
  {:id :wasm32-kotoba-v1,
   :role "Portable wasm32-kotoba-v1 bytes with shared KIR meaning",
   :status :required-for-pure-product},
  :js-kotoba-v1
  {:id :js-kotoba-v1,
   :role "Restricted ESM / js-kotoba-v1 target",
   :status :optional-expanding},
  :native-x86_64
  {:id :native-x86_64,
   :role "Pure native AOT subset (i64/string/option)",
   :status :future-t1.4},
  :native-aarch64
  {:id :native-aarch64,
   :role "Pure native AOT subset (i64/string/option)",
   :status :future-t1.4},
  :host-clj
  {:id :host-clj,
   :role "Clojure host embed for :clj expect keys / .clj entries",
   :status :host-compat},
  :host-cljs
  {:id :host-cljs,
   :role
   "ClojureScript host embed for :cljs expect keys / .cljs entries",
   :status :host-compat},
  :compiler-admit
  {:id :compiler-admit,
   :role
   "Frontend admit/reject for negative and extension-error cases",
   :status :required-for-negative}},
 :case-classes
 {:pure-product-run
  {:required-backends #{:kir :wasm32-kotoba-v1},
   :optional-backends #{:native-x86_64 :js-kotoba-v1 :native-aarch64},
   :profile :pure-product,
   :kinds #{:run},
   :note
   "T1.3 gate: both required backends must pass; no KIR-only silent pass"},
  :compile-expr
  {:required-backends #{:kir :wasm32-kotoba-v1},
   :optional-backends #{:js-kotoba-v1},
   :profile :pure-product,
   :kinds #{:compile-expr},
   :note
   "Inline expression cases share pure-product dual-backend bar"},
  :host-reader-target
  {:required-backends #{:host-cljs :host-clj},
   :optional-backends #{:kir},
   :profile :host-compat,
   :kinds #{:run},
   :note
   "Reader-conditional / multi-target expect; not pure-product dual-backend gate"},
  :host-entry-extension
  {:required-backends #{},
   :optional-backends #{:kir :wasm32-kotoba-v1 :host-cljs :host-clj},
   :profile :host-compat,
   :kinds #{:run},
   :note
   "Per-case required-backends derived from entry extension + expect keys"},
  :negative-admit
  {:required-backends #{:compiler-admit},
   :optional-backends #{},
   :profile :security,
   :kinds #{:expect-error :extension-error},
   :note "Admission reject fixtures; no execution backends"},
  :compiler-run
  {:required-backends #{:kir},
   :optional-backends #{:wasm32-kotoba-v1 :js-kotoba-v1},
   :profile :compiler,
   :kinds #{:run},
   :note "Sugar whose grammar entry is :backends #{:compiler}: the frontend lowers it and KIR executes the lowering. Not a pure-product case -- the profile refuses these heads -- so it never stands in for the dual-backend gate."}},
 :cases
 [{:id :reader-target-branching,
   :kind :run,
   :entry "reader_target/branching.cljc",
   :function "main",
   :args [41],
   :expect {:kotoba 51, :clj 42, :cljs 43},
   :class :host-reader-target,
   :required-backends #{:kir :host-cljs :host-clj}}
  {:id :reader-target-default-fallback,
   :kind :run,
   :entry "reader_target/default_fallback.cljc",
   :function "main",
   :args [7],
   :expect {:kotoba 107, :clj 107, :cljs 107},
   :class :host-reader-target,
   :required-backends #{:kir :host-cljs :host-clj}}
  {:id :reader-conditional-splicing,
   :kind :run,
   :entry "reader_target/splicing.cljc",
   :function "main",
   :args [0],
   :expect {:kotoba 3, :clj 1, :cljs 2},
   :class :host-reader-target,
   :required-backends #{:kir :host-cljs :host-clj}}
  {:args [1],
   :required-backends #{:kir :host-cljs :host-clj},
   ;; :clj measured 2026-08-12 by kotoba.lang.host-clj-conformance-test. The
   ;; Clojure loader resolves demo.util to src/demo/util.clj (bump = +1), so
   ;; (run 1) is 2. It was recorded as 11 -- the value the .cljc variant (+10)
   ;; gives, which is right for :cljs, where no .clj exists. The :clj entry had
   ;; been copied from :cljs by symmetry rather than measured, and this is the
   ;; one case whose whole purpose is to distinguish them. :kotoba and :cljs
   ;; are unchanged and not measured here.
   :expect {:kotoba 101, :clj 2, :cljs 11},
   :source-paths ["namespace_priority/src"],
   ;; The entry defines `run`, not `main`. Recorded as "main" until 2026-08-12,
   ;; when the first runner to execute this case resolved the symbol and found
   ;; only `run`. Nothing had run it, so nothing had disagreed.
   :function "run",
   :id :namespace-extension-priority,
   :class :host-reader-target,
   :kind :run,
   :entry "namespace_priority/main.cljc"}
  {:id :entry-extension-kotoba,
   :kind :run,
   :entry "entry_extensions/main.kotoba",
   :function "main",
   :args [5],
   :expect {:kotoba 15},
   :class :host-entry-extension,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :entry-extension-cljk,
   :kind :run,
   :entry "entry_extensions/main.cljk",
   :function "main",
   :args [5],
   :expect {:kotoba 20},
   :class :host-entry-extension,
   :required-backends #{:kir}}
  {:id :entry-extension-clj,
   :kind :run,
   :entry "entry_extensions/main.clj",
   :function "main",
   :args [5],
   :expect {:clj 25},
   :class :host-entry-extension,
   :required-backends #{:host-clj}}
  {:id :entry-extension-cljc,
   :kind :run,
   :entry "entry_extensions/main.cljc",
   :function "main",
   :args [5],
   :expect {:kotoba 35, :clj 35, :cljs 35},
   :class :host-entry-extension,
   :required-backends #{:kir :host-cljs :host-clj}}
  {:id :entry-extension-cljs,
   :kind :run,
   :entry "entry_extensions/main.cljs",
   :function "main",
   :args [5],
   :expect {:cljs 45},
   :class :host-entry-extension,
   :required-backends #{:host-cljs}}
  {:id :inline-expression-compile,
   :kind :compile-expr,
   :expr "(+ 1 2)",
   :function "main",
   :args [],
   :expect {:kotoba 3, :clj 3, :cljs 3},
   :class :compile-expr,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  ;; The first two cases in this manifest to record WHERE each required
  ;; backend is actually driven. Measured 2026-09-03: this case declared
  ;; `#{:kir :wasm32-kotoba-v1}` and was executed on NEITHER. amu's
  ;; `kotoba.compiler.lang-conformance` drives exactly those two backends but
  ;; only over its own `resources/kotoba/lang-conformance/pilot-manifest.edn`,
  ;; which does not declare it, and never reads this file; `kotoba`'s
  ;; `kotoba.language-conformance-test` does read this file, but drives
  ;; `kotoba.runtime/wasm-binary`, a backend `:backends` above does not name,
  ;; through its own pinned copy of this repository. The case was declared on
  ;; two backends and exercised on a third that was not declared, and nothing
  ;; compared the two facts. `:executed-by` and `:unexecuted-backends` are
  ;; checked against `:required-backends` by
  ;; `kotoba.lang.conformance-matrix/validate-execution`, and the runner named
  ;; below asserts in the other direction that the case it just ran says so.
  {:id :bounded-set-literal-and-operations,
   :kind :run,
   :entry "collections/set.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 1},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1},
   :executed-by
   {:kir "kotoba-lang/kotoba-lang kotoba.lang.collections-conformance-test"},
   :unexecuted-backends
   {:wasm32-kotoba-v1
    {:as-of "2026-09-03",
     :reason
     "No runner in this repository emits wasm32-kotoba-v1: the `:test` alias
      depends on kotoba-sema, which produces HIR and drives KIR, and not on
      amu, which owns the wasm32 backend. amu HAS a dual-backend runner for
      :kir and :wasm32-kotoba-v1 -- `kotoba.compiler.lang-conformance` -- but
      it reads its own pilot manifest, which does not declare this case.",
     :closes-when
     "amu's dual-backend runner reads this manifest, or this repository gains
      a wasm32 runner; either way the entry moves to :executed-by, and leaving
      it here once it is executed is itself a failure."}}}
  ;; A set literal is `[:set :keyword]` and nothing else. This is the exact
  ;; shape `collections/set.kotoba` carried until 2026-09-03, when measuring it
  ;; for the first time showed it could not be admitted at all; keeping it as a
  ;; negative is what stops the rule from being re-learned by accident.
  {:id :heterogeneous-set-literal-is-refused,
   :kind :expect-error,
   :entry "collections/set_heterogeneous.kotoba",
   :target :kotoba,
   :error-contains "set item type mismatch",
   :class :negative-admit,
   :required-backends #{:compiler-admit},
   :executed-by
   {:compiler-admit
    "kotoba-lang/kotoba-lang kotoba.lang.collections-conformance-test"}}
  {:id :bounded-vector-literal-and-operations,
   :kind :run,
   :entry "collections/vector.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 20},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :bounded-named-higher-order-collections,
   :kind :run,
   :entry "collections/higher_order.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 24},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :nested-let-destructuring,
   :kind :run,
   :entry "collections/destructuring.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 29},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :record-protocol-static-dispatch,
   :kind :run,
   :entry "records/basic.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 16},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :named-multi-arity-and-variadic-functions,
   :kind :run,
   :entry "functions/multi_arity.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 25},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :first-class-closures-invoke-and-apply,
   :kind :run,
   :entry "functions/closures.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 37},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :contextual-computed-invoke,
   :kind :run,
   :entry "functions/contextual_invoke.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 2},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :canonical-list-closure-result,
   :kind :run,
   :entry "functions/closure_list_result.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 2},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :canonical-bytes-closure-result,
   :kind :run,
   :entry "functions/closure_bytes_result.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 1},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :public-callable-contracts,
   :kind :run,
   :entry "functions/callable_contracts.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 7},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :pure-call-by-name-lazy-sequences,
   :kind :run,
   :entry "functions/lazy_sequences.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 11},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :portable-string-symbol-values,
   :kind :run,
   :entry "values/string_symbol.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 10},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :bounded-control-match-and-pure-desugar,
   :kind :run,
   :entry "control/match_desugar.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 21},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :portable-case-and-threading-sugar,
   :kind :run,
   :entry "control/case_threading.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 51},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :portable-option-presence-sugar-absence,
   :kind :run,
   :entry "control/some_sugar.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 3},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :portable-negative-and-advanced-threading-sugar,
   :kind :run,
   :entry "control/simple_sugar.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 26},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :portable-bounded-dotimes,
   :kind :run,
   :entry "control/dotimes.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 7},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :portable-bounded-vector-literal-doseq,
   :kind :run,
   :entry "control/doseq.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 7},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :portable-guest-assert,
   :kind :run,
   :entry "control/assert.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 7},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :portable-closed-world-multimethod,
   :kind :run,
   :entry "control/multimethod.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 121},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :portable-static-predicate-condp,
   :kind :run,
   :entry "control/condp.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 50},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:args [],
   :required-backends #{:kir :wasm32-kotoba-v1},
   :expect {:kotoba 9},
   :function "main",
   ;; A two-module PROJECT, not a prelude. `:prelude` was this case's key
   ;; until 2026-09-01 and nothing in amu ever read it; the entry now carries
   ;; `(:require [stdlib.core :as core])` and resolves it the ordinary way.
   :source-paths ["."],
   :id :portable-source-stdlib,
   :class :pure-product-run,
   :kind :run,
   :entry "stdlib/basic.kotoba"}
  {:args [],
   :required-backends #{:kir :wasm32-kotoba-v1},
   ;; One term per stdlib version-3 name, each on its own power of ten, so a
   ;; wrong answer names the function. Measured on wasm32 2026-09-02.
   :expect {:kotoba 678392943530},
   :function "main",
   :source-paths ["."],
   :id :portable-source-stdlib-extended,
   :class :pure-product-run,
   :kind :run,
   :entry "stdlib/extended.kotoba"}
  {:args [],
   :required-backends #{:kir :wasm32-kotoba-v1},
   ;; stdlib.sorted-map, the persistent ordered map written in Kotoba: one
   ;; term per public name on its own power of ten, so a wrong answer names
   ;; the function. The fold is worth 13 and takes two decades.
   ;; Measured on wasm32 2026-09-02 (2557 bytes, amu ca869d79e).
   :expect {:kotoba 61332520143},
   :function "main",
   :source-paths ["."],
   :id :portable-source-stdlib-ordered,
   :class :pure-product-run,
   :kind :run,
   :entry "stdlib/ordered.kotoba"}
  {:args [],
   :required-backends #{:kir :wasm32-kotoba-v1},
   ;; The two names version 5 added to stdlib.core -- frequencies, which
   ;; returns a canonical [:map :i64 :i64], and get-in2, which reads through
   ;; a two-level one. Both were recorded as unwritable under the manifest's
   ;; :absent until the integer key reached the friendly surface.
   ;; Measured on wasm32 2026-09-02 (5126 bytes, amu a8e30d5b with
   ;; kotoba-sema cc2f5139 and kotoba-kir ad6db332).
   :expect {:kotoba 3555086133},
   :function "main",
   :source-paths ["."],
   :id :portable-source-stdlib-keyed,
   :class :pure-product-run,
   :kind :run,
   :entry "stdlib/keyed.kotoba"}
  {:id :fuel-bounded-collections-beyond-eight,
   :kind :run,
   :entry "performance/collections_12.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 103},
   :class :pure-product-run,
   :required-backends #{:kir :wasm32-kotoba-v1}}
  {:id :negative-malformed-reader-conditional,
   :kind :expect-error,
   :entry "negative/malformed_reader_conditional.cljc",
   :target :kotoba,
   :error-contains
   "reader conditional requires feature/expression pairs",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  {:id :negative-runtime-require-denied,
   :kind :expect-error,
   :entry "negative/runtime_require.cljc",
   :target :kotoba,
   :safe true,
   :error-contains "require",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  {:id :negative-unsupported-extension,
   :kind :extension-error,
   :entry "negative/not_source.txt",
   :error-contains "unsupported source extension",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  ;; Abort ability, slice 1 (lang/abort-ability.edn). Negative cases are pinned
  ;; by the EXACT rejection message: test/kotoba/lang/abort_conformance_test.clj
  ;; requires equality, not containment, so a refusal that fires for another
  ;; reason cannot count as this one.
  {:id :abort-slice-1-throw-and-try-in-one-function,
   :kind :run,
   :entry "abort/throw_try_one_function.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 7},
   :class :compiler-run,
   :required-backends #{:kir}}
  {:id :abort-slice-1-abort-through-a-callee,
   :kind :run,
   :entry "abort/abort_through_callee.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 104},
   :class :compiler-run,
   :required-backends #{:kir}}
  {:id :abort-slice-1-explicit-catch-type,
   :kind :run,
   :entry "abort/explicit_catch_type.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 16},
   :class :compiler-run,
   :required-backends #{:kir}}
  {:id :abort-slice-1-negative-two-error-types,
   :kind :expect-error,
   :entry "abort/negative/two_error_types.kotoba",
   :target :kotoba,
   :safe true,
   :error-contains "function f throws two different error types: string and i64",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  ;; Slice 2 retired :abort-slice-1-negative-call-outside-try. The program is
  ;; the same; the refusal moved, because the call is now admitted and the
  ;; PROPAGATION reaches the export boundary. Reaching it there is what says
  ;; the propagation happened.
  {:id :abort-slice-2-negative-propagated-abort-at-export,
   :kind :expect-error,
   :entry "abort/negative/propagated_abort_at_export.kotoba",
   :target :kotoba,
   :safe true,
   :error-contains "unhandled abort at export boundary; catch it with try: main",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  {:id :abort-slice-1-negative-unhandled-at-export,
   :kind :expect-error,
   :entry "abort/negative/unhandled_at_export.kotoba",
   :target :kotoba,
   :safe true,
   :error-contains "unhandled abort at export boundary; catch it with try: main",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  {:id :abort-slice-1-negative-throw-inside-loop,
   :kind :expect-error,
   :entry "abort/negative/throw_inside_loop.kotoba",
   :target :kotoba,
   :safe true,
   :error-contains "throw inside a loop/doseq/dotimes body is not admitted in abort slice 1: precondition :checked-lexical-facet-unwind is not met",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  {:id :abort-slice-1-negative-throw-in-facet-scope,
   :kind :expect-error,
   :entry "abort/negative/throw_in_facet_scope.kotoba",
   :target :kotoba,
   :safe true,
   :error-contains "throw in function f whose effect row has a dataspace facet operation #{:dataspace/transact} is not admitted in abort slice 1: precondition :checked-lexical-facet-unwind is not met",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  {:id :abort-slice-1-negative-nothing-to-catch,
   :kind :expect-error,
   :entry "abort/negative/nothing_to_catch.kotoba",
   :target :kotoba,
   :safe true,
   :error-contains "try body cannot abort; there is nothing to catch",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  ;; Abort ability, slice 2: propagation through a caller that throws nothing
  ;; itself, an aborting form in operand and test position, and a :document
  ;; error type. Pinned by exact message, like slice 1.
  {:id :abort-slice-2-propagate-through-caller,
   :kind :run,
   :entry "abort/propagate_through_caller.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 5},
   :class :compiler-run,
   :required-backends #{:kir}}
  {:id :abort-slice-2-operand-position,
   :kind :run,
   :entry "abort/operand_position.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 108},
   :class :compiler-run,
   :required-backends #{:kir}}
  {:id :abort-slice-2-ex-info-round-trip,
   :kind :run,
   :entry "abort/ex_info_round_trip.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 11},
   :class :compiler-run,
   :required-backends #{:kir}}
  {:id :abort-slice-2-negative-propagated-error-type-mismatch,
   :kind :expect-error,
   :entry "abort/negative/propagated_error_type_mismatch.kotoba",
   :target :kotoba,
   :safe true,
   :error-contains "function g throws i64 but calls aborting function `f`, which aborts with string",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  {:id :abort-slice-2-negative-two-callees-disagree,
   :kind :expect-error,
   :entry "abort/negative/two_callees_disagree.kotoba",
   :target :kotoba,
   :safe true,
   :error-contains "function g calls aborting functions with two different error types: string (`f`) and i64 (`h`)",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  {:id :abort-slice-2-negative-aborting-call-inside-loop,
   :kind :expect-error,
   :entry "abort/negative/aborting_call_inside_loop.kotoba",
   :target :kotoba,
   :safe true,
   :error-contains "call to aborting function `f` inside a loop/doseq/dotimes body is not admitted in abort slice 1: precondition :checked-lexical-facet-unwind is not met",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  {:id :abort-slice-2-negative-throw-in-fn-literal-operand,
   :kind :expect-error,
   :entry "abort/negative/throw_in_fn_literal_operand.kotoba",
   :target :kotoba,
   :safe true,
   :error-contains "throw inside a fn literal is not admitted in abort slice 1: precondition :checked-lexical-facet-unwind is not met",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  ;; Local state, slice 1 (lang/local-state.edn). Negative cases are pinned by
  ;; the EXACT rejection message: test/kotoba/lang/local_state_conformance_test.clj
  ;; requires equality, not containment, so a fixture refused for another
  ;; reason -- an unbound symbol, a type error in the fixture itself -- cannot
  ;; count as the refusal it was written to demonstrate.
  {:id :local-state-slice-1-counter-in-a-let,
   :kind :run,
   :entry "local-state/counter_in_a_let.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 42},
   :class :compiler-run,
   :required-backends #{:kir}}
  {:id :local-state-slice-1-atom-through-an-if,
   :kind :run,
   :entry "local-state/atom_through_an_if.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 15},
   :class :compiler-run,
   :required-backends #{:kir}}
  {:id :local-state-slice-1-swap-with-extra-arguments,
   :kind :run,
   :entry "local-state/swap_with_extra_arguments.kotoba",
   :function "main",
   :args [],
   :expect {:kotoba 42},
   :class :compiler-run,
   :required-backends #{:kir}}
  {:id :local-state-slice-1-negative-escapes-as-an-argument,
   :kind :expect-error,
   :entry "local-state/negative/escapes_as_an_argument.kotoba",
   :target :kotoba,
   :safe true,
   :error-contains "atom `a` escapes its let scope (atom slice 1 admits swap!/reset!/deref in straight-line code of the binding function only)",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  {:id :local-state-slice-1-negative-escapes-into-a-fn,
   :kind :expect-error,
   :entry "local-state/negative/escapes_into_a_fn.kotoba",
   :target :kotoba,
   :safe true,
   :error-contains "atom `a` escapes its let scope (atom slice 1 admits swap!/reset!/deref in straight-line code of the binding function only)",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  {:id :local-state-slice-1-negative-read-inside-a-loop,
   :kind :expect-error,
   :entry "local-state/negative/read_inside_a_loop.kotoba",
   :target :kotoba,
   :safe true,
   :error-contains "atom `a` escapes its let scope (atom slice 1 admits swap!/reset!/deref in straight-line code of the binding function only)",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  {:id :local-state-slice-1-negative-swap-changes-the-type,
   :kind :expect-error,
   :entry "local-state/negative/swap_changes_the_type.kotoba",
   :target :kotoba,
   :safe true,
   :error-contains "atom `a` is i64; this rebinding is string (atom slice 1 requires one type per cell)",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}
  {:id :local-state-slice-1-negative-deref-of-a-non-cell,
   :kind :expect-error,
   :entry "local-state/negative/deref_of_a_non_cell.kotoba",
   :target :kotoba,
   :safe true,
   :error-contains "deref expects a let-bound atom cell as its first argument; got `x` (atom slice 1)",
   :class :negative-admit,
   :required-backends #{:compiler-admit}}]}
