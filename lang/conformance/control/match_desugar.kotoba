(defdesugar clamp [x lo hi]
  (if (< x lo) lo (if (> x hi) hi x)))

(defn main []
  (+ (loop [n 5 acc 0]
       (if (= n 0) acc (recur (- n 1) (+ acc n))))
     (match {:value 9}
       {:value n} (clamp n 0 6)
       :else 0)))
