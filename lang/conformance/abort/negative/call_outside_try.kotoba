(ns kotoba.lang.conformance.abort-negative (:export [main]))
;; refused: call to aborting function `f` must be inside try or in a function that aborts with the same error type
(defn- f [x :i64] :i64 (if (= x 0) (throw "s") x))
(defn main [] :i64 (f 1))
