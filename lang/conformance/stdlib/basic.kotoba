(ns stdlib.basic
  (:export [main])
  (:require [stdlib.core :as core]))

(defn main []
  (+ (core/first-match (fn [x] (> x 3)) (pair 1 (pair 4 (pair 9 0))))
     (pair-first (core/reverse (pair 5 0)))))
