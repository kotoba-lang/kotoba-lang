(defn make-adder [n]
  (fn [x] (+ x n)))

(defn add [a b]
  (+ a b))

(defn main []
  (+ (invoke (make-adder 5) 7)
     (+ (invoke (fn-ref add) 3 8)
        (apply (fn [a b c] (+ a (+ b c))) 1 [2 3]))))
