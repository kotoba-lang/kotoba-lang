name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  installed-kotoba-pilot:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Verify migrated CLJC kernel with released native Kotoba
        shell: bash
        run: |
          set -euo pipefail
          release_dir="$RUNNER_TEMP/kotoba-v0.7.1"
          cli_dir="$RUNNER_TEMP/kotoba-cli"
          mkdir -p "$release_dir" "$cli_dir"
          curl -L --fail --silent --show-error \
            -o "$release_dir/kotoba-linux-amd64.tar.gz" \
            https://github.com/kotoba-lang/kotoba/releases/download/v0.7.1/kotoba-linux-amd64.tar.gz
          curl -L --fail --silent --show-error \
            -o "$release_dir/kotoba-linux-amd64.tar.gz.sha256" \
            https://github.com/kotoba-lang/kotoba/releases/download/v0.7.1/kotoba-linux-amd64.tar.gz.sha256
          (cd "$release_dir" && sha256sum -c kotoba-linux-amd64.tar.gz.sha256)
          tar -xzf "$release_dir/kotoba-linux-amd64.tar.gz" -C "$cli_dir"
          "$cli_dir/kotoba" compile lang/migration-pilots/demo-util.kotoba \
            --target web --output "$RUNNER_TEMP/demo-util.mjs" --json
          grep -q 'kotoba.artifact/provenance' "$RUNNER_TEMP/demo-util.mjs.manifest.edn"
          node scripts/test-installed-kotoba-pilot.mjs "$RUNNER_TEMP/demo-util.mjs"
          if "$cli_dir/kotoba" compile lang/migration-pilots/negative-eval.kotoba \
            --target web --output "$RUNNER_TEMP/forbidden.mjs" --json; then
            echo "expected forbidden eval to be rejected" >&2
            exit 1
          fi

  pure-product-examples:
    # T2.3: every pure-product example under examples/ compiles + KIR-executes
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Checkout Amu sibling
        run: |
          git clone --depth 1 https://github.com/kotoba-lang/amu ../amu
          # Amu tip (dd943e08, 2026-08-25) fails to load:
          # kotoba/compiler/frontend.cljc references kir/host-stack-exhausted?
          # which is absent from the KIR that alias resolves. Pin to the last
          # tip that compiled this job (18564bce, #607, 2026-08-23).
          git -C ../amu fetch --depth 1 origin 18564bcec7b6c98414b3bcf53e7455be1d0f8154
          git -C ../amu checkout --detach 18564bcec7b6c98414b3bcf53e7455be1d0f8154
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - uses: DeLaGuardo/setup-clojure@13.4
        with:
          cli: latest
      - name: Run pure-product examples (T2.3)
        env:
          KOTOBA_LANG_ROOT: ${{ github.workspace }}
        run: clojure -M:pure-product-examples

  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Checkout qualification siblings
        run: |
          git clone --depth 1 https://github.com/kotoba-lang/kotoba ../kotoba
          git clone --depth 1 https://github.com/kotoba-lang/amu ../amu
          git clone --depth 1 https://github.com/kotoba-lang/kotoba-sema ../kotoba-sema
          git clone --depth 1 https://github.com/kotoba-lang/kotoba-core-contracts ../kotoba-core-contracts
          # These pins name commits whose VENDORED bytes match this repo's
          # authority. Advanced 2026-09-05 after :host/fs-browse merge 0b320850:
          # guest-grammar sha256
          # 9d701ea9a803a4b3d7dc4245274a9a901ab4ac506ebd401282b2acdf7747dd9c
          #   kotoba 8113792f (main) matches both copies
          # capability-catalog sha256
          # e191c8b166b44a1cc55cdd13c45e333121fef95d8ad3bee4a1f61a7733ea56fc
          #   kotoba-sema 04e0c79c (main) matches catalog+grammar
          # host-parity 3740c866 still matches.
          # amu 249b9408 and kotoba-core-contracts 5ce5b174 have no
          # guest-grammar copy at all (HTTP 404, not an empty file) -- the
          # test tolerates :missing. Verified 2026-09-06 by asking for the
          # path's size rather than hashing what came back: a 404 hashed as
          # the empty string, which is a real sha256 and reads as DRIFT.
          # kotoba 8113792f (main) matches guest-grammar 3e41eb84. Measured
          # 2026-09-06: the previous pin dd8cf587 carried 9d701ea9 -- the same
          # stale hash kotoba-sema's pin carried, because both were advanced
          # together on 2026-09-05 and the authority moved after that.
          git -C ../kotoba fetch --depth 1 origin 8113792fdf2bdaca727095720950cb93b9967dd2
          git -C ../kotoba checkout --detach 8113792fdf2bdaca727095720950cb93b9967dd2
          # amu moves WITH kotoba. Measured 2026-09-06: advancing kotoba alone
          # to 8113792f made amu's pinned frontend reject the newer portable
          # stdlib with `expression type mismatch: expected vector-i64, got
          # i64` (:subset-reject) in eleven tests. The compiler and the stdlib
          # it compiles are one set; a partial advance is not a smaller change,
          # it is an incoherent one.
          git -C ../amu fetch --depth 1 origin b5a0c302917cee7d49e4ac50545b2fb46358664f
          git -C ../amu checkout --detach b5a0c302917cee7d49e4ac50545b2fb46358664f
          # kotoba-sema 04e0c79c (main) matches guest-grammar 3e41eb84 and catalog e191c8b1.
          # Measured 2026-09-06 by hashing both files at both shas:
          #   authority guest-grammar 3e41eb84 | 86565528 -> 9d701ea9 DRIFT | 04e0c79c -> MATCH
          #   authority catalog       e191c8b1 | 86565528 -> 0ac56e8e DRIFT | 04e0c79c -> MATCH
          # The old comment was accurate when written; the authority moved
          # afterwards and this pin did not, so `test` -- a REQUIRED check --
          # had been failing on every main commit with
          # `local-and-sibling-vendors-match-authority`. Advancing a sibling's
          # vendored copy is only half the job; the pin that CI reads is the
          # other half.
          git -C ../kotoba-sema fetch --depth 1 origin 04e0c79c42a66cd0d3728615d73d071f449c3410
          git -C ../kotoba-sema checkout --detach 04e0c79c42a66cd0d3728615d73d071f449c3410
          git -C ../kotoba-core-contracts fetch --depth 1 origin 5ce5b174feeca1cb24d8266b20dcd0a685500ebf
          git -C ../kotoba-core-contracts checkout --detach 5ce5b174feeca1cb24d8266b20dcd0a685500ebf
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - uses: DeLaGuardo/setup-clojure@13.4
        with:
          cli: latest
          bb: latest
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install nbb
        run: npm install -g nbb
      - run: clojure -M:test
      - name: Compatibility report (T10.2)
        run: |
          mkdir -p artifacts
          clojure -M:compatibility | tee artifacts/compatibility-report.edn
      - uses: actions/upload-artifact@v4
        with:
          name: compatibility-report
          path: artifacts/compatibility-report.edn
      - name: Verify checked XMILE maturity result
        run: clojure -M:maturity --check
      - run: clojure -M:lint
      - run: bb scripts/check-cli-contract.bb lang/cli.edn
      - run: bb scripts/check-capability-values.bb
      - run: bb scripts/check-legacy-runtime-absence.bb
