# Examples

## Pure-product living contract (T2.3)

Manifest: [`pure-product-examples.edn`](./pure-product-examples.edn)

```bash
# requires sibling ../amu checkout
clojure -M:pure-product-examples
```

CI job `pure-product-examples` runs the same command. See
`docs/adr/ADR-reliability-t23-pure-product-examples-ci.md`.
