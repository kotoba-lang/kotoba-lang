lang: register fs/browse-dir capability in the catalogue (wire id 261)

Adds :fs/browse-dir (compiler-wire-id 261, source-op fs/browse-dir,
effect :host/fs-browse-dir) to the capability catalogue after :fs/browse
(34). This is the capability the kotoba-lang/find recursive tree-walk
lowers; it pairs with kotoba-core-contracts 2ff3f736 (capability id 261)
and the :host/fs-browse-dir effect row already merged in the grammar
resync wave. Both the lang/ authority and the resources/ copy updated
together (two files in one commit, per vendored-copies discipline).