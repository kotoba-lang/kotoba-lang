;; jvm-dep-ledger.edn — JVM dependency Q9 migration ledger (jvm-dep-migrator)
;; Machine authority: lang/q9-migration.edn (v3) + ADR-q9-whole-component-build-migration.md
;; Classification per repo: has deps.edn; migrated = migration-plan.y*ml or migration*.edn record;
;; cljc-both = src/**/*.cljc present; native-only = .clj/.cljs only; no-source = neither.
{:jvm-dep-ledger/version 1
 :as-of "2026-09-03"
 :organization "kotoba-lang"
 :unit :whole-component
 :inventory
 {:repos-with-deps-edn 2036
  :migrated 5
  :cljc-both 1975
  :native-only 33
  :no-source 23}
 :migrated-repos [annotation app-comfyui com-aadhaar* ooyake runpod]  ;; com-aadhaar* = blocked record, see below
 :wave-1-cohort-known 20
 :wave-1-cohort-found 23
 :wave-1-cohort
  {"com-8th-wall" :cljc-both
   "com-aadhaar" :migrated
   "com-aave" :cljc-both
   "com-abb-800xa" :cljc-both
   "com-abb-robotics" :cljc-both
   "com-accela" :cljc-both
   "com-accelbyte" :cljc-both
   "com-ach-network" :cljc-both
   "com-acorrd-standards" :cljc-both
   "com-acquia" :cljc-both
   "com-adobe" :cljc-both
   "com-ads-b-aviation" :cljc-both
   "com-adyen" :cljc-both
   "com-aftership" :cljc-both
   "com-agleader" :cljc-both
   "com-agones" :cljc-both
   "com-agora" :cljc-both
   "com-airbus-analytx" :cljc-both
   "com-airbyte" :cljc-both
   "com-airtable" :cljc-both
   "com-algolia" :cljc-both
   "com-alpaca" :cljc-both
   "com-anthropic" :cljc-both}
 :blocked-components
  [{:repository "orgs/kotoba-lang/com-aadhaar"
    :component "aadhaar.main whole surface -> src/aadhaar/whole-component.kotoba"
    :disposition :blocked
    :as-of "2026-09-03"
    :authority "lang/q9-migration.edn v3 :whole-component-build-contract"
    :gates "all 4 fail-closed; no JVM fallback; evidence migration-evidence/gates-20260903.txt in orgs/kotoba-lang/com-aadhaar @21c0c2a (branch bot/migrate-com-aadhaar-20260903)"
    :gap "amu --jvm-free admitted subset lacks: string literals/lowering, str concat, count, reduce/mapv/filterv/reduce-kv/keys/remove/min/seq/some->, parse-long, (long x), #() fn literals, (:k m) keyword projection, (contains? m :k) on maps; capability-import syntax for clock/uuid/mutable-store undeclared"
    :next "re-run probe matrix after any amu frontend landing that extends the admitted subset; do NOT shrink to decision-core"}]
 :next-component
  "tranche-1/2 cohort repos share the same CRUD shape (strings + folds + clock/uuid/store TCB);
   they are blocked for the same measured reason until the subset grows. Next whole-component
   candidate: re-scan org for a pure int/map leaf component after amu subset expansion."
 :method "/tmp/classify.sh one-pass find-based classification, 2026-09-03; regenerate on each tick"
}
